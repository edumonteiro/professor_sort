<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="row">
      <div class="col-md-3 dropdown" style="margin-top: 2%">
        <% if current_professor %>
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Menu <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><%= link_to 'My Preferences', professor_preferences_path, method: :get %></li>
          <li><%= link_to 'My Lectures', lectures_professor_path, method: :get %></li>
          <li><a href="#">Something else here</a></li>
          <li role="separator" class="divider"></li>
          <li><a href="#">Separated link</a></li>
          <li role="separator" class="divider"></li>
          <li><a href="#">One more separated link</a></li>
        </ul>
        <% end %>
      </div>
      <div class="col-md-6">
        <% if current_professor && current_professor.kind == 'admin'%> 
          <h4 id="top-name"><%= link_to 'EasySchedule',admin_path, method: :get %></h4>
        <% elsif %>
          <h4 id="top-name"><%= link_to 'EasySchedule',professor_preferences_path, method: :get %></h4>
        <% else %>
          <h4 id="top-name"><%= link_to 'EasySchedule',new_session_path, method: :get %></h4>
        <% end %>
      </div>
      <div class="col-md-3" style="margin-top: 2%">
        <ul class="nav nav-tabs">
          <div class='col-md-4'>
            <li role="presentation" class="active">
              <h8><%= link_to 'Login', new_session_path, method: :get %></h8>
            </li>
          </div>
          <div class='col-md-4'>
            <li>
              <h8><%= link_to "Logout", session_path("current"), method: :delete %></h8>
            </li>
          </div>
          <div class='col-md-4'>
            <li>
              <h8><%= link_to "Signup", new_professor_path, method: :get %></h8>
            </li>
          </div>
        </ul>
      </div>
    </div> 
  </div>
</nav>
